events {
    worker_connections 1024;
}

http {
    server {
        listen 8080;

        location / {
            include fastcgi_params;
            proxy_pass http://server:81;
        }
        
        location /status {
            stub_status on;
        }
    }
}