apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: service
  name: ${HOTEL_NAME}
spec:
  replicas: 1
  strategy:
    type: Recreate 
  selector:
    matchLabels:
      app: ${HOTEL_NAME}
  template:
    metadata:
      labels:
        app: ${HOTEL_NAME}
    spec:
      containers:
      - name: ${HOTEL_NAME}
        image: xedll/hotel-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: ${HOTEL_SERVICE_PORT}
        env:
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_HOST
        - name: POSTGRES_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_PORT
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: POSTGRES_PASSWORD
        - name: POSTGRES_DB
          value: "hotels_db"
        - name: AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: AUTH_TOKEN
---
apiVersion: v1
kind: Service
metadata:
  namespace: service
  name: ${HOTEL_NAME}
spec:
  selector:
    app: ${HOTEL_NAME}
  ports:
  - port: ${HOTEL_SERVICE_PORT}
    targetPort: ${HOTEL_SERVICE_PORT}