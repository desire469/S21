apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${SESSION_NAME}
  namespace: service
spec:
  replicas: 1
  strategy:
    type: Recreate 
  selector:
    matchLabels:
      app: ${SESSION_NAME}
  template:
    metadata:
      labels:
        app: ${SESSION_NAME}
    spec:
      containers:
      - name: ${SESSION_NAME}
        image: xedll/session-service:latest  
        imagePullPolicy: IfNotPresent 
        ports:
        - containerPort: ${SESSION_SERVICE_PORT}
        env:
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_HOST
        - name: POSTGRES_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_PORT
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: POSTGRES_PASSWORD
        - name: POSTGRES_DB
          value: "users_db"
        - name: AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: AUTH_TOKEN
---
apiVersion: v1
kind: Service
metadata:
  namespace: service
  name: ${SESSION_NAME}
spec:
  selector:
    app: ${SESSION_NAME}
  ports:
  - port: ${SESSION_SERVICE_PORT}
    targetPort: ${SESSION_SERVICE_PORT}